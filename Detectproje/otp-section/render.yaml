services:
  - type: web
    name: safe-nation-otp
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    healthCheckPath: /health
    pythonVersion: 3.9
    envVars:
      - key: PORT
        value: 10000
